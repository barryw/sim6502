# Cocogitto config - used for local commit validation only
# CI calculates versions from conventional commits directly

from_latest_tag = true
tag_prefix = "v"
branch_whitelist = ["main"]
ignore_merge_commits = true

[changelog]
path = "CHANGELOG.md"
