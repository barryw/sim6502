# Archived Plans

These files are historical design and implementation plans. They are useful for understanding how features were built, but they are not the current API reference.

For current behavior, use the repository root `README.md` and the implementation in `sim6502/`. In particular, CLI flags, backend protocol tool names, and generated code snippets in older plans may have changed after implementation.
